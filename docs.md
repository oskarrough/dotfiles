# Documentation and tips

## Fish shell

Here are some quick tips for installing and using fish shell on OSX and Linux. 

Install with `brew install fish`.

Set the default shell to fish:  `chsh -s `which fish``.

Install plugins with [Fisher](https://github.com/jorgebucaran/fisher#install).
